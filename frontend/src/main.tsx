import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>tidemarq — frontend coming in Phase 5</div>
  </React.StrictMode>,
)

services:
  tidemarq:
    build: .
    ports:
      - "8716:8716"
      - "8717:8717"
    volumes:
      - tidemarq_data:/data
    environment:
      - TIDEMARQ_CONFIG=/etc/tidemarq/tidemarq.yaml
    restart: unless-stopped

volumes:
  tidemarq_data:

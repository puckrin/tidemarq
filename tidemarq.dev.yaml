server:
  http_port: 8080
  https_port: 8443
  static_dir: ""

database:
  path: ./dev-data/tidemarq.db

tls:
  cert_file: ./dev-data/certs/server.crt
  key_file: ./dev-data/certs/server.key

auth:
  jwt_secret: "dev-secret-do-not-use-in-prod"
  jwt_ttl: 24h

admin:
  username: admin
  password: admin
